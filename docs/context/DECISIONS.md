# DECISIONS

> 目的: 継続して参照する判断を短く残す。仕様本文の正本は `docs/spec_*.md` とする。

## Decisions

- D-20260528-009 | `RAU-SALES-03` では、sales / ADR adapter と baseline forecast を `src/curveCore.ts` の pure function として実装する。追加する runtime contract は `SalesAdrObservation`、`UnitPriceForecastV1Candidate`、`SalesForecastV1Candidate`、`buildSalesAdrInputFromBookingCurveResponses()`、`buildUnitPriceForecastResult()`、`buildSalesForecastResult()` とする。`api_current_adr_baseline:v1` は asOfDate 時点の ADR を predictedAdr として返す baseline に限定し、historical ADR baseline は optional field の実データ分布を見てから後続判断にする。`rooms_x_unit_price:v1` は rooms forecast の `predictedFinalRooms` と unit price forecast の `predictedAdr` を掛け合わせるだけにし、rooms forecast を内部で再計算しない。UI、API request、IndexedDB schema、rank recommendation scoring はこの task では変更しない | status: active | spec_link: docs/spec_002_curve_core.md
- D-20260528-008 | `RAU-SALES-02` では、sales / ADR を rooms 用 `CurveInput` へ混ぜず、別 adapter が `SalesAdrObservation` を作る契約にする。`booking_curve_raw_source:v2` の保存単位は追加変更しない。ADR は Revenue Assistant の `*_adr` field を第一候補にし、欠損時だけ `sales_sum / room_sum` で計算する。0 室では ADR を推測せず、売上 0 と ADR 0 は欠損と同一視しない。単価予測は `UnitPriceForecastV1Candidate`、売上予測は rooms forecast と unit price forecast を合成する `SalesForecastV1Candidate` として、UI、API 取得、IndexedDB から分離した pure function で後続実装する。rank recommendation へ接続する場合も、top list へ数値を直接表示せず、ADR 悪化、sales 悪化、RevPAR 相当悪化のような非数値 reason / diagnostics から段階導入する | status: active | spec_link: docs/spec_002_curve_core.md, docs/spec_003_rank_recommendation_signal.md
- D-20260528-007 | `RAU-FC-05` では、forecast diagnostics を rank recommendation scoring の priority / confidence 補助へ接続する。接続は追加 API 取得ではなく、候補生成時に `booking_curve_raw_source:v2` の roomGroup response を読み、`scope="roomGroup"`、`segment="transient"` の `ForecastResult v1 candidate` を作る形にする。raw source 内の `last_year_room_sum`、`two_years_ago_room_sum`、`three_years_ago_room_sum` から作る raw history reference は、top list scoring 補助用の内部入力であり、BCL-tuned reference curve そのものとは扱わない。raw history reference の `ACT` は、同じ raw source 内の `0日前` 過去年平均が取れる場合だけ作り、取れない場合は final rooms を推測しない。live の将来 stayDate では `actual_final_missing` を evaluation dataset 上の除外理由として残すが、予測生成の blocking missing reason にはしない。forecast signal は `high_occupancy`、`low_occupancy`、`neutral` の内部分類に留め、top list には forecast 数値を表示しない。forecast 欠損時は既存 reference deviation scoring を継続し、recommendedRank 名表示、Revenue Assistant write、bulk apply は追加しない | status: active | spec_link: docs/spec_002_curve_core.md, docs/spec_003_rank_recommendation_signal.md
- D-20260528-006 | `RAU-FC-04` では、first rooms-only forecast model を `src/curveCore.ts` の pure function として実装する。既定 model は `recent_deviation_adjusted_seasonal:v1` とし、計算式は `seasonalFinalRooms + (currentRooms - recentRoomsAtObservedLt)` とする。これは、季節型 reference curve の final rooms 推定値を土台にし、同じ LT の現在値と直近型 reference curve の差分で補正する単純モデルである。比較用 baseline として `seasonal_ratio_baseline:v1` も同じ関数で返せるようにし、計算式は `currentRooms / (seasonalRoomsAtObservedLt / seasonalFinalRooms)` とする。どちらも UI、API、storage、rank recommendation scoring へはまだ接続しない。欠損時は `predictedFinalRooms=null` とし、diagnostics に missing reason、source count、`actSeparated`、`smallCapacity`、`groupDriven` を残す。capacity がある場合は 0 以上 capacity 以下に丸め、`expectedOccupancyRatio` を返す | status: active | spec_link: docs/spec_002_curve_core.md
- D-20260528-005 | `RAU-FC-03` では、forecast evaluation dataset の初期実装を `src/curveCore.ts` の pure function として置く。UI、Revenue Assistant API 呼び出し、IndexedDB schema、Tampermonkey 表示には接続しない。理由は、`RAU-FC-02` の評価契約は core logic の入力、出力、diagnostics の契約であり、まず `CurveInput` から `ForecastEvaluationCase` を作れることが、first forecast model を評価可能にする最小条件だからである。`buildForecastEvaluationCase()` は `actualFinalRooms` を評価 target、`observedPrefix` を `asOfDate` 時点の入力として分離し、`actual_final_missing`、`observed_prefix_missing`、`future_info_required`、`act_not_separated`、`room_group_id_missing`、`segment_unknown` を diagnostics として返す。`summarizeForecastEvaluationResults()` は `ForecastResultV1Candidate` と case の組から `maeRooms`、`smape`、`biasRooms`、rank recommendation impact proxy を集計する。これにより、`RAU-FC-04` は UI や storage に依存せず first forecast model を pure function として実装できる | status: active | spec_link: docs/spec_002_curve_core.md
- D-20260528-004 | `RAU-FC-02` では、rooms-only forecast の評価契約を `docs/spec_002_curve_core.md` に確定した。evaluation dataset の grain は `facilityId x targetStayDate x asOfDate x scope x roomGroupId? x segment` とし、forecast model の入力には `asOfDate` 時点で観測済みの `observedPrefix` と reference curve だけを使う。`actualFinalRooms`、user decision、rank change label は評価 target または evaluation proxy としてだけ使い、future final rooms、未来の rank change、未来の競合価格を入力特徴量へ混ぜない。`ForecastResult v1 candidate` は `facilityId`、`modelId`、`modelVersion`、target、scope、segment、observedLt、currentRooms、capacityRooms、predictedFinalRooms、expectedOccupancyRatio、任意の predictedCurve、diagnostics を持つ proposed contract とする。初期 metrics は `maeRooms`、`smape`、`biasRooms` とし、`smape` は予測と実績がどちらも 0 の場合 0、片方だけ 0 の場合は最大 2.0 を上限にする。rank recommendation impact proxy は `priorityOrderChangedCount`、`dismissedProxyCount`、`snoozedProxyCount`、`resolvedByRankChangeProxyCount` に限定し、いずれも真の正解ラベルとして扱わない。`snoozed_by_user` は false positive ではなく、利用者が「見たが今は触らない」と判断した一時判断ログである | status: active | spec_link: docs/spec_002_curve_core.md, docs/spec_003_rank_recommendation_signal.md
- D-20260528-003 | `RAU-FC-01` では、rooms-only forecast model を今すぐ実装して rank recommendation scoring へ接続するのではなく、先に forecast evaluation dataset、metrics、`ForecastResult v1 candidate` の input / output / diagnostics contract を設計する。A「今すぐ forecast 実装へ進む」は、final occupancy expectation を priority / confidence に使える可能性があり実務価値はあるが、現時点では過去 stayDate による誤差、bias、小キャパでの暴れ、団体起因の上振れ誤変換、UI 上の current / reference curve / forecast / 推奨ランク方向の混同を検証できていないため採用しない。C「当面は reference deviation + rank response + sales/ADR health だけで進め forecast は見送る」は、first wave の安全性は高いが、現在 pace だけではなく最終着地見込みで候補優先度を補強する余地、rank response 評価の反実仮想 baseline として使う余地を捨てるため採用しない。B「forecast 評価 dataset / metrics を先に設計する」を採用し、`RAU-FC-02` を next task とする。forecast は、評価済みで diagnostics が許容できる場合だけ priority / confidence の補助要素にし、top list へ forecast 数値を直接表示しない。初期評価で使う `snoozed_by_user`、`dismissed_by_user`、`resolved_by_rank_change` は真の正解ラベルではなく evaluation proxy として扱う。特に `snoozed_by_user` は false positive ではなく、利用者が「見たが今は触らない」と判断した一時判断ログである | status: active | spec_link: docs/spec_002_curve_core.md, docs/spec_003_rank_recommendation_signal.md
- D-20260528-002 | `RAU-RR-09` から `RAU-RR-11` では、rank response、recommendedRank、bulk apply を first phase の実装範囲と将来候補へ分ける。rank response dataset は `/api/v3/lincoln/suggest/status` の rank change event と `booking_curve_raw_source:v2` の rooms / sales / ADR を接続し、価格弾力性ではなく `ランク反応度` として設計する。recommendedRank は current rank と rank ladder 候補が取得できる場合でも、`rank_sequences[].default_sequence` の方向が Revenue Assistant UI の上げ / 下げに対応することを確認するまでは出さず、`recommendedRankDirection` を主表示にする。bulk apply は current rank、rank ladder、reflection_allow、write endpoint 候補までは見えているが、request shape、安全制約、partial failure、同時更新、preview、選択状態、反映結果保存が未確認または未実装であるため、`not-now` とし first phase では button も API 実行も追加しない | status: active | spec_link: docs/spec_003_rank_recommendation_signal.md
- D-20260528-001 | `RAU-RR-03` の Chrome DevTools Protocol read-only 調査では、`/api/v1/suggest/output/current_settings` から `stayDate x roomGroup` 単位の current rank (`latest_current.price_rank_code` / `latest_current.price_rank_name`) を取得できること、`/api/v1/rank_sequences` から rank ladder 候補 (`price_rank_code`、`price_rank_name`、`default_sequence`) を取得できること、`/api/v1/rank_colors` から rank 表示色候補を取得できることを確認した。一方で、`rank_sequences[].default_sequence` の大小が rank 上げ / 下げのどちらに対応するかは未確認であり、`/api/v1/plan_master/plan_rank_price` の観測 field には実価格や金額 field が見えなかったため、rank price table、現在販売中価格、プラン別・人数別・食事条件別価格は未確認のまま扱う。`/api/v1/lincoln/suggest/reflection_allow` は `suggest_calc_datetime` 指定で `is_allowed` を返すが、write safety の証明ではない。bundle 内に rank 反映系の POST endpoint 候補は見つかったが実行しておらず、request shape、権限、error response、partial failure、同時更新時の挙動は未確認である。したがって `RAU-RR-04` の first UI shell では current rank 名は表示候補にできるが、recommendedRank 名は direction 確認まで必須表示にしない | status: active | spec_link: docs/spec_003_rank_recommendation_signal.md
- D-20260527-007 | `RAU-RR-02` では、`/api/v4/booking_curve` の保存前 compact を廃止して response 全文を保存するのではなく、`compactBookingCurveResponse()` の保持対象を rooms、sales、ADR の RAU 利用 field へ拡張する。保存 schema version は `booking_curve_raw_source:v2` に上げる。cache key に schema version が含まれるため、既存 `booking_curve_raw_source:v1` record は新しい read / write path では読まれず、次回取得時に v2 record が作られる。IndexedDB object store と index 構造は変えないため、IndexedDB database version は 1 のまま据え置く。トップカレンダーの保存済み raw source signal も現行 schema version の record だけを有効扱いにし、v1 rooms-only record を sales / ADR 保存済みと誤読しないようにする。この repo で `booking_curve raw source` と呼ぶ保存単位は、Revenue Assistant response 全文や trace 全文ではなく、facility、stayDate、asOfDate、scope、roomGroup、endpoint、query、schema version と、RAU が扱う rooms / sales / ADR fields を保持する保存契約である | status: active, amends D-20260527-004 | spec_link: docs/spec_001_analyze_expansion.md, docs/spec_002_curve_core.md, docs/spec_003_rank_recommendation_signal.md
- D-20260527-006 | rank recommendation は forecast そのものではなく、RM の作業キューとして扱う。forecast が使えるようになると priority / confidence の精度は上がるが、first wave は reference curve deviation と既存 booking curve で開始できる。`RAU-FC-01` とは関連するが、rank recommendation は UI、candidate lifecycle、user decision、rank response、future bulk apply まで含むため独立 bundle とする。scoring では `all` だけでなく `transient` / `group` を分け、group が主因の全体上振れは個人向け rank 上げ根拠として抑制する。極小キャパは 1 室の影響が大きいため、`判定対象外` または低 confidence とし、判定不能やデータ不足は diagnostics / reason code として残す | status: active | spec_link: docs/spec_003_rank_recommendation_signal.md
- D-20260527-005 | 選択範囲の一括反映は将来候補として残すが、rank recommendation first phase では非目標にする。一括反映を検討できる条件は、current rank、rank ladder、rank 反映 endpoint、request shape、安全制約、user decision、cooldown、resolved、dismissed の状態管理が確認または実装されていることである。実装する場合も自動反映ではなく user-confirmed bulk apply とし、反映直前の current rank 再取得、recommendation 生成後の別 rank change 確認、snoozed / dismissed / cooldown / low confidence / small capacity / group-driven 候補の除外、隣接 rank 限定、全件 preview、明示選択行だけの反映、部分失敗記録を必須 guardrail とする | status: active | spec_link: docs/spec_003_rank_recommendation_signal.md
- D-20260527-004 | sales / ADR は `/api/v4/booking_curve` response に含まれることが確認済みであるため、rank response、ADR / sales health、将来の単価予測や売上予測に使う第一候補は booking_curve raw source とする。rooms だけを見ると、値下げで pickup が増えても ADR や sales が悪化したケースを良い反応と誤解しやすいため、rank response 評価では rooms、ADR、sales、RevPAR 相当を合わせて見る。ただし 2026-05-27 のコード現状確認では `src/main.ts` の `compactBookingCurveResponse()` が rooms 系列だけを残し、sales / ADR を落としている。したがって、raw source と呼ぶ保存契約を維持するには、`RAU-RR-02` で schema version を上げるか既存 compact の責務を変えるかを実装前に判断し、sales / ADR を落とさない保存契約へ更新する | status: active, amended-by D-20260527-007 | spec_link: docs/spec_003_rank_recommendation_signal.md
- D-20260527-003 | rank recommendation は user snooze と様子見 cooldown を持つ。様子見は false positive ではなく、利用者が「見たが今は触らない」と判断したログである。対応不要は false positive 候補として扱い、同じ `stayDate x roomGroup x action x reasonFingerprint` の再表示を抑制する。様子見と対応不要を分ける理由は、前者は一時抑制、後者は同じ根拠での再表示抑制と model 改善 input であり、後続の bulk apply 対象除外にも使うためである。reasonFingerprint は、同じ根拠の繰り返し通知と、新しい根拠による再通知を分けるために使う | status: active | spec_link: docs/spec_003_rank_recommendation_signal.md
- D-20260527-002 | トップ画面にはカレンダー badge だけではなく、料金調整候補リストを追加する。badge は日付セルに状態があることを示す補助表示には向くが、複数日と複数部屋タイプを比較して作業順を決めるには不足する。候補リストは `stayDate x roomGroup` を原則単位にし、priority、宿泊日、部屋タイプ、現ランク、推奨方向、主要根拠、状態、Analyze 導線、様子見、対応不要を行として表示する。Analyze は詳細確認の場とし、将来的には対象 roomGroup card の展開、scroll、highlight へ接続する | status: active | spec_link: docs/spec_003_rank_recommendation_signal.md
- D-20260527-001 | rank recommendation first wave は推奨レート金額ではなく、推奨ランク方向を中心にする。推奨レート金額には、プラン別料金、人数別料金、食事条件、販売中価格、rank ladder、ランク別価格表、競合価格、施設方針などが必要であり、first phase の確度では断定的な金額を出す責任が大きい。Revenue Assistant の実操作が販売 rank 変更中心であるなら、`維持`、`上げ検討`、`下げ注意`、`監視`、`判定対象外` のような離散判断として提示するほうが、現在のデータ確認範囲と操作単位に合う。current rank と rank ladder が取得できると確認できた場合だけ、`1段階上げ検討` や `1段階下げ検討` のような隣接 rank recommendation へ進める | status: active | spec_link: docs/spec_003_rank_recommendation_signal.md
- D-20260514-001 | Browserbase `browser-trace` / `browser-to-api` は、この repo では optional API discovery workflow として扱う。新しい画面、新しいタブ、未調査 API、response shape が不明な API を扱うときに利用を検討する。`browser-to-api` の出力は公式 API 仕様ではなく、観測済み通信から作った推定資料として扱い、実装前に endpoint、query parameter、request / response sample、null 許容、optional field、empty array、error response、pagination、日付範囲、権限差、confidence を確認する。Revenue Assistant 内の自分の契約アカウント、自施設、自分の権限内、read-only、画面補助または分析補助の API 利用と、OTA、競合サイト、第三者サイトの hidden API 利用は明確に分ける。raw trace、HAR、request / response body、credential、個人情報、予約情報、価格・在庫の非公開データは commit しない。Yellow 以上の API 利用は、利用目的、保存範囲、削除方針、負荷、権限確認を `DECISIONS.md`、対象 spec、または task docs に記録してから実装する | status: active | spec_link: AGENTS.md
- D-20260502-002 | `RAU-SALES-02` は直近の表示活用 task ではなく、将来の予測モデル側へ寄せる。`RAU-SALES-01` で、売上と ADR が既存 `/api/v4/booking_curve` response に含まれることを確認済みである。2026-05-27 の `RAU-RR-02` で、IndexedDB raw source の保存契約は rooms / sales / ADR fields を保持する `booking_curve_raw_source:v2` へ更新済みである。そのため、直近では売上・ADR の追加取得 queue を増やさず、既存 booking_curve raw source を単価予測と売上予測へ接続する adapter / model 設計を後続で扱う | status: active, amended-by D-20260527-004, D-20260527-007 | spec_link: docs/spec_002_curve_core.md, docs/spec_003_rank_recommendation_signal.md
- D-20260502-001 | Analyze 日付単位の売上と ADR は、新規 endpoint ではなく既存の `/api/v4/booking_curve` raw source を第一候補として扱う。Chrome CDP 調査で、`/api/v4/booking_curve?date=20260430` と `rm_room_group_id` 指定の両方について、`all`、`transient`、`group` に `this_year_sales_sum`、`last_year_sales_sum`、`two_years_ago_sales_sum`、`three_years_ago_sales_sum`、`this_year_adr`、`last_year_adr` が含まれることを確認した。月次 `/api/v1/booking_curve/monthly` の `sales_based` は月単位の予約日基準系列として有効だが、Analyze の stay_date 単位判断では `/api/v4/booking_curve` の既存 raw source を使う方が保存単位、scope、room group、asOfDate を揃えやすい。後続実装では、新しい売上取得 queue を作る前に、既存 booking curve raw source の adapter を将来の単価予測と売上予測で使える形へ拡張する | status: active | spec_link: docs/spec_001_analyze_expansion.md
- D-20260501-001 | 競合価格の部屋タイプ別データは、指定なし response の後処理だけでは不足する。Chrome CDP 調査で、`/api/v5/competitor_prices` に `jalan_room_types[]=TWIN` を単独指定すると、指定なし response には含まれなかった TWIN plan が返ることを確認した。部屋タイプ条件として効く query は `jalan_room_types[]` であり、`jalan_facility_room_types[]`、`jalan_facility_room_type`、`room_types[]` は効かなかった。複数部屋タイプを同時指定しても各部屋タイプを網羅せず、指定集合内の最安値寄りに絞られるため、部屋タイプ別 snapshot は `SINGLE`、`DOUBLE`、`TWIN`、`TRIPLE`、`FOUR_BEDS` を単独 request として保存する。ただし、従来の `指定なし` snapshot は廃止しない。`指定なし` response には、Revenue Assistant の部屋タイプ絞り込み選択肢に独立して存在しない `SEMI_DOUBLE` や raw room type が空のその他相当 plan が最安値として含まれる場合があるためである。競合価格グラフの Tooltip には、施設名、実際に返った部屋タイプ、価格、前回差分を表示する | status: active | spec_link: docs/spec_001_analyze_expansion.md
- D-20260430-003 | `/api/v4/booking_curve` response 全体を localStorage に永続保存する旧 persistent cache は廃止する。Chrome CDP 確認では、localStorage 全体約 5.18 MB のうち `revenue-assistant:group-room-count:v4:<facility>:booking-curve:` 配下 36 件が約 5.16 MB を占め、`QuotaExceededError` の主因になっていた。同じ raw source は IndexedDB に保存済みであり、localStorage 側は memory cache と役割が重複するため、新規読み書きを止める。既存 key は facility prefix に限定して削除し、group-room result cache、booking curve raw source IndexedDB、derived reference curve IndexedDB、競合価格 snapshot IndexedDB は削除対象にしない | status: active | spec_link: docs/spec_001_analyze_expansion.md
- D-20260430-002 | 競合価格 snapshot は、Analyze 日付ページを開いた時点で保存候補にするだけでなく、利用者が Analyze 画面内で競合価格タブを開いた場合は、その stay_date の競合価格確認意図がより明確になったものとして取得優先度を上げる。ただし、booking_curve warm cache の queue に全日付分の競合価格取得を混ぜない。競合価格 snapshot は、現在開いている stay_date と現在の検索条件 signature に限定して、in-flight dedupe と連続取得抑制を維持した専用の保存状態として扱う。Analyze 画面の indicator には、booking_curve warm cache とは別に、競合価格 snapshot の保存状態、最終保存時刻、前回 snapshot 有無、保存失敗または競合施設未設定による skip を表示する | status: active | spec_link: docs/spec_001_analyze_expansion.md
- D-20260430-001 | 競合価格 endpoint は Analyze 日付ページで `GET /api/v5/competitor_prices` として呼ばれる。request には `x-requested-with: XMLHttpRequest` が必要で、query には少なくとも `date`、`min_num_guests`、`max_num_guests`、`yad_nos[]` が必要である。`meal_types[]` は省略可能で、省略すると素泊まり、朝食、夕食、朝夕食を含む広めの response を取得できる。`min_num_guests=1`、`max_num_guests=6`、`meal_types[]` 省略の request は `200` で取得できたが、response に実際に入る人数は該当プランが存在する人数だけである。`yad_nos[]` なしの全競合取得、宿泊人数範囲なしの取得、`max_num_guests=10` の広い取得は `400 BAD_REQUEST` になった。response は `own` と `competitors` を持ち、各 plan は人数、食事条件、プラン名、じゃらん部屋タイプ、URL、価格、自社価格との差分を持つが、在庫状態、販売停止、満室、ページング情報は持たない。初期 snapshot は、競合施設一覧と宿泊人数範囲を含む検索条件 signature ごとに保存する。競合施設は最大 5 件で後から入れ替え可能なため、保存時点の `yad_nos[]` と競合施設名を snapshot に保存し、施設単位の価格推移は `yad_no` ごとに追跡する | status: active | spec_link: docs/spec_001_analyze_expansion.md
- D-20260429-007 | 競合価格推移 snapshot は、全件取得を前提にしない。Analyze 日付ページを開いた日付や、料金判断のために繰り返し確認された日付ほど snapshot 履歴が厚くなる設計を第一候補にする。最初の作業は、Revenue Assistant に保存されている検索条件を無視して、絞り込みなし、空条件、または初期条件に近い request で競合価格 data を取得できるかの調査とする。response に人数、食事条件、部屋タイプ、プラン、在庫状態などが含まれ、保存後に RAU 側で絞り込めるなら広めの raw snapshot 保存を候補にする。response が画面条件に強く依存するなら検索条件 signature ごとに別系列保存とする | status: active | spec_link: docs/spec_001_analyze_expansion.md
- D-20260429-006 | warm cache の通常対象は、31 日では短いため、トップカレンダーでは `as_of_date - 1日` から `as_of_date + 3か月` までへ広げる。`as_of_date - 1日` を含める理由は、直近で実績確定した ACT を raw source として保存し、`0日前` と `ACT` の分離や reference curve 評価に使える証跡を増やすためである。API 負荷は、同時取得 1、request 間隔 1.0 秒以上、1 回最大 10 分、3 分クールダウン、document hidden pause、連続エラー 3 回停止で抑える。一時的な失敗に対しては同じ task を最大 2 回まで queue 末尾へ戻して retry する。トップカレンダー由来のクールダウン中でも、Analyze 日付ページを開いた場合は、利用者が見ている stay_date を優先する queue を作り直して取得を再開する | status: active | spec_link: docs/spec_001_analyze_expansion.md
- D-20260429-005 | warm cache indicator の対象日数は、数値だけでは対象期間を誤読しやすいため、最小表示にも対象日付範囲を含める。完了日数は reference curve と同曜日補助線まで含む重い完了定義を維持するが、完了前でも進行中の日付が分かるように `進行 n日` とカレンダーセル下端の細い色 line を追加する。line は、一部取得済みを青、完了を緑、取得エラーありを赤とする | status: active | spec_link: docs/spec_001_analyze_expansion.md
- D-20260429-004 | reference curve の `0日前` は、raw source 保存開始前の過去 stay_date では `ACT` と分離できない場合があるため、core logic、derived reference curve cache、予測評価用の入力値では推測補完しない。一方で画面上の参考線としては、`0日前` が欠損している、または `ACT` と同値で API 側の実績上書き混入が疑われる場合に限り、表示層で `1日前` と `ACT` の線形補間値を整数室数に丸めて描画してよい。この値は保存せず、Tooltip では補完値であることを明示する | status: active | spec_link: docs/spec_001_analyze_expansion.md, docs/spec_002_curve_core.md
- D-20260429-003 | booking_curve warm cache は、参考線込みの完了定義に対して従来設定が安全寄りすぎるため、request 間隔を 1.0 秒以上、1 回の自動稼働を最大 10 分、クールダウンを 3 分以上へ緩和する。安全弁として、同時 warm cache task 実行は 1、reference curve request scheduler の同時数制限、有限 queue、document hidden 中の一時停止、連続エラー 3 回停止を維持する。IndexedDB または derived cache の既存 record で skip できる task は API request を発行しないため、次 task へ即時に進める | status: active | spec_link: docs/spec_001_analyze_expansion.md
- D-20260429-002 | booking_curve warm cache の完了定義は、current 用 raw source だけではなく、直近型と季節型の derived reference curve、derived reference curve の計算に必要な reference source raw source、直近同曜日補助線に必要な `-14日`、`-7日`、`+7日`、`+14日` の raw source まで含める。Analyze 日付ページを開いている場合は、開いている stay_date、その週、その月、通常 warm cache 範囲の順に取得優先度を上げる。同曜日補助線は前後週の raw source が揃っていれば表示時に整形して描画できるため、derived cache を必須にしない | status: active | spec_link: docs/spec_001_analyze_expansion.md
- D-20260429-001 | booking_curve warm cache は、1 日合計稼働時間の上限を設けない。API 負荷は、対象を `asOfDate + 0日` から `asOfDate + 30日` までに限定する有限 queue、同時取得 1、request 間隔 2.5 秒以上、1 回の自動稼働最大 5 分、10 分クールダウン、document hidden 中の一時停止、連続エラー停止で抑える。日次上限を撤廃する理由は、有限 queue とクールダウンにより連続 request が抑えられ、日をまたがない長時間閲覧時でも未取得分を少しずつ補えるため | status: active | spec_link: docs/spec_001_analyze_expansion.md
- D-20260426-004 | booking_curve warm cache は、Analyze 日付ページだけでなくトップカレンダー表示中にも起動対象にする。Indicator は task 件数だけでなく stay_date 単位の完了状況を表示し、同じ stay_date のホテル全体と全室タイプが取得済みまたは skip 済みになった日付を完了済み範囲として扱う。1 回の自動稼働上限に達した場合は、クールダウン後に自動再開する。document hidden、連続エラー停止は自動取得を止める条件として維持する | status: active, amended-by D-20260429-001 | spec_link: docs/spec_001_analyze_expansion.md
- D-20260426-003 | booking_curve warm cache は、部屋タイプや系列を優先するのではなく、近い stay_date からホテル全体と全室タイプを揃える順序で取得する。初期対象は `today + 0日` から `today + 30日` までとし、同じ stay_date 内ではホテル全体、全室タイプの順に取得する。差分更新は、前回 response との差分だけを保存することではなく、現在の `as_of_date` で未保存の raw source key だけを取得することを指す。同じ `facilityId + stayDate + asOfDate + scope + roomGroupId + endpoint + query + schema` が IndexedDB に存在する場合は skip し、`as_of_date` が変わった場合は別 snapshot として保存する。API 負荷を抑えるため、初期実装では同時取得 1、request 間隔 2.5 秒以上、1 回の自動稼働最大 5 分とし、取得状況 indicator を表示する | status: active, amended-by D-20260429-001, D-20260429-002 | spec_link: docs/spec_001_analyze_expansion.md
- D-20260426-002 | Analyze booking curve の次段階では、`団体` は常時3枚目の panel として増やさず、`個人` を既定表示にしたうえで `個人 / 団体` を切り替えられる toggle として追加する。`直近同曜日カーブ` は `直近型カーブ` の平均線が妥当かを確認する補助線として扱い、既定 OFF、前後2週、薄いグレー破線、hover 時に対象日付を確認できる表示を候補にする。競合価格は現在値を表示するだけなら Revenue Assistant 標準タブで足りるため、RAU では現在値表ではなく、取得時点つきの価格 snapshot を IndexedDB に保存して価格推移を追跡する後続候補として扱う | status: active | spec_link: docs/spec_001_analyze_expansion.md
- D-20260426-001 | Analyze booking curve の `0日前` と `ACT` は、値が同じ場合でも別概念として扱う。`0日前` は宿泊日当日時点で取得した予約状態、`ACT` は宿泊日後に確定した最終実績を指す。Revenue Assistant API が過去 stay_date の `0日前` を実績確定後の値で上書きして返す場合、raw source 保存開始前の過去日程は後から分離できないため、推測で補完しない。レスポンス改善、`0日前` と `ACT` の分離、将来の予測評価 dataset の入力証跡を兼ねて、`/api/v4/booking_curve` raw source を IndexedDB 保存対象にする。reference curve の表示範囲は current と同じ `0〜360日前 + ACT` を目標にする | status: active | spec_link: docs/spec_001_analyze_expansion.md, docs/spec_002_curve_core.md
- D-20260424-006 | Booking curve の core logic は、Analyze 画面 UI、Revenue Assistant API client、localStorage、IndexedDB から分離した純粋処理として実装する。core logic の正本は `docs/spec_002_curve_core.md` とし、`stay_date x LT` matrix、BCL-tuned reference curve、将来の予測モデル、将来の予測評価が共通で使う input / output / diagnostics をそこで定義する。予測モデルと予測評価は今後の候補として視野に入れるが、`RAU-AF-04` の完了条件にはせず、まず reference curve と evaluation-ready な入出力契約を固める | status: active | spec_link: docs/spec_002_curve_core.md
- D-20260424-005 | Analyze booking curve reference curve の算出ロジックは、BCL repo の booking curve 画面で使う考え方を参照して RAU 向けにチューニングする。`直近型カーブ` は対象 stay_date の直前 7 泊日中央値ではなく、同じ曜日の履歴を LT ごとの 90 日窓で集約し、直近ほど重みを大きくする `recent90w` 相当を第一候補にする。`季節型カーブ` は `/api/v4/booking_curve` response 内の `last_year_room_sum` をそのまま線にせず、前年と 2 年前の同月・同曜日の履歴から final rooms に対する LT 比率を作り、その比率と final rooms 推定値から rooms-only reference curve を作る。BCL Python 実装、PMS データ、外部 DB は RAU first wave の必須入力にしない。request 数増加に備え、表示用に圧縮した derived reference curve は IndexedDB 保存を前提にし、in-flight dedupe、同時 request 数制限、室タイプ別 card の on-demand 取得を組み合わせる | status: active | spec_link: docs/spec_001_analyze_expansion.md
- D-20260424-004 | Analyze booking curve reference curve の first wave 仮定義では、`直近型カーブ` は対象 `stay_date` の直前 7 泊日を比較対象にし、各 LT tick の rooms 値は非 null 値の中央値で集約する。`季節型カーブ` は `/api/v4/booking_curve` が返す `last_year_stay_date`、各 point の `last_year_date`、`last_year_room_sum` を優先し、欠損時だけ `two_years_ago_room_sum`、`three_years_ago_room_sum` の順で補う。2026-04-24 の BCL repo 再確認により、この定義は BCL 想定と異なるため、実装済み UI shell の仮ロジックとしてのみ扱い、今後の仕様ターゲットからは外す | status: superseded-by D-20260424-005 | spec_link: docs/spec_001_analyze_expansion.md
- D-20260424-003 | `/api/v4/booking_curve` は、少なくとも 2026-04-24 時点のログイン済み Revenue Assistant 環境では、ホテル全体と全 6 室タイプについて、確認した比較対象 `stay_date` と `rm_room_group_id` の組み合わせで 200 応答を返す。response は `booking_curve`、`stay_date`、`last_year_stay_date`、`max_room_count` を持ち、各 point は `date`、`last_year_date`、`all`、`transient`、`group` を持つ。`batch-date` は response には含まれないため、既存の同期文脈または cache key 側の値として扱う | status: active | spec_link: docs/spec_001_analyze_expansion.md
- D-20260424-002 | `repo-template-codex` の共通断片に合わせ、RAU でも `INTENT`、`STATUS`、`DECISIONS`、`tasks_backlog`、`spec` の責務分離を明確化する。次スレッド入口は `STATUS`、実行順は `tasks_backlog`、複数判断に使う優先順位は `INTENT` を正とする | status: active | spec_link: AGENTS.md, docs/context/INTENT.md, docs/context/STATUS.md, docs/tasks_backlog.md, docs/spec_000_overview.md
- D-20260424-001 | 当面の主線は、RAR 側の本格分析ツールではなく、RAU を `レート調整特化 + 人数なしの簡易フォーキャスト` として進める。Analyze 日付ページの日別 booking curve へ、BCL の `直近型カーブ` と `季節型カーブ` に相当する rooms-only reference curve を、ホテル全体と室タイプ別の両方で表示できるようにする | status: active | spec_link: docs/spec_000_overview.md, docs/spec_001_analyze_expansion.md
- D-20260331-001 | リポジトリ名は `revenue-assistant-userscript` とし、単一機能専用ではなくレベニューアシスタント向け拡張基盤として扱う | status: applied | spec_link: docs/spec_000_overview.md
- D-20260331-002 | 初回実装は UI 装飾より、`booking_curve` 取得、キャッシュ、再同期の土台を優先する | status: applied | spec_link: docs/spec_001_analyze_expansion.md
- D-20260411-001 | 個別リポジトリ側の文書構成は `repo-template-codex` の現行 `AGENTS.md` / `STATUS.md` / `DECISIONS.md` 形式へ寄せ、リポジトリ固有ルールは `Local Extension` と `docs/spec_*.md` に残す | status: applied | spec_link: AGENTS.md
- D-20260411-002 | 団体室数系キャッシュは `最終データ更新` 日付だけでなく施設単位でも分離し、起動時、ページ復帰時、フォーカス復帰時の整合チェックで異常を検知したら group 系キャッシュを破棄して再同期する | status: applied | spec_link: docs/spec_001_analyze_expansion.md
- D-20260417-001 | 販売設定タブ内の室タイプ別 booking curve はフェーズ分割とし、Phase 1 は `室数` のみ、baseline なしで進め、標準 UI は `全体` と `個人` の 2 系列を横並び表示する | status: applied | spec_link: docs/spec_001_analyze_expansion.md
- D-20260417-002 | 室タイプ別 booking curve の LT 圧縮は bucket 集約とし、代表値は平均ではなく各 bucket の最後の日を使い、`ACT` は `0日前` と分離して扱う | status: applied | spec_link: docs/spec_001_analyze_expansion.md
- D-20260417-003 | Phase 1 では `localStorage` へ booking curve 生 JSON を persistent 保存せず、必要なら最小系列のみを保持し、`IndexedDB` は baseline 導入以降に再判断する | status: applied | spec_link: docs/spec_001_analyze_expansion.md
- D-20260417-004 | 最上段の全体 block はホテル全体 booking curve を常時展開で表示し、各室タイプ card は既定で閉じ、開閉トリガーは各 block 自身に持たせる | status: applied | spec_link: docs/spec_001_analyze_expansion.md
- D-20260417-005 | Phase 1 の booking curve UI は custom SVG の placeholder 実装を先行し、hover tooltip と capacity 基準 y 軸を含めて見た目と操作を先に固める | status: applied | spec_link: docs/spec_001_analyze_expansion.md
- D-20260417-006 | Phase 1 の横軸ラベルは 2 段表示へ固定せず、1 行表示を優先し、優先表示ラベルは `ACT, 3, 7, 14, 21, 30, 45, 60, 90, 120, 150, 180, 270, 360` とする | status: applied | spec_link: docs/spec_001_analyze_expansion.md
- D-20260417-007 | 室タイプ別 booking curve の rank 変更履歴は、Phase 1 では各室タイプ card のみへ小さな丸 marker で重ね、同日複数変更は最後の 1 件だけを tooltip 付きで表示する | status: applied | spec_link: docs/spec_001_analyze_expansion.md
- D-20260418-001 | 販売設定タブの室タイプ別 `1日前 / 7日前 / 30日前` の販売室数差分は、Phase 1 では `/api/v4/booking_curve` の室タイプ別 `all.this_year_room_sum` を正として維持し、販売設定系 endpoint への寄せ替えは Phase 2 以降の再判断とする | status: applied | spec_link: docs/spec_001_analyze_expansion.md
- D-20260418-002 | 現行 current UI の synthetic room-type host における個別 booking curve capacity は `/api/v1/suggest/output/current_settings` の `rm_room_groups[].max_num_room` を room group 名単位で補い、既存 card renderer を再利用する | status: applied | spec_link: docs/spec_001_analyze_expansion.md
- D-20260418-003 | `queueCalendarSync()` の MutationObserver 起点は、observer callback ごとに直接 queue せず、同期が空くまで 1 本だけ待たせてから queue し、debug snapshot は console だけでなく DOM と localStorage にも残す | status: applied | spec_link: docs/spec_001_analyze_expansion.md
- D-20260418-004 | observer coalescing と completed-signature 打ち切り後の focus 復帰 GUI 実測では `mutation-observer` が requested 1 / scheduled 1 に留まり、現時点では `queueCalendarSync()` の性能改善は一区切りとする。次は `同月同曜日` baseline の最小範囲を決め、`IndexedDB` はその実装 scope が固まってから booking_curve persistent cache 単位で再判断する | status: applied | spec_link: docs/spec_001_analyze_expansion.md
- D-20260418-005 | analyze のランク変更履歴は、次の slice では最新1件サマリーを維持し、table へ `増減` 列だけを追加する最小差分で進める | status: applied | spec_link: docs/spec_001_analyze_expansion.md
- D-20260418-006 | トップカレンダーの最終変更表示は、初期 slice では `相対日数のみ / セル最下部のみ / analyze 画面では非表示` を正とし、文字密度を優先して日付文字列や常時表示は入れない | status: applied | spec_link: docs/spec_001_analyze_expansion.md
- D-20260418-007 | 月次実績画面のカスタム booking curve は、いきなり実装へ入らず、まず対象画面の DOM と API を調査してから最小仕様を決める | status: applied | spec_link: docs/spec_000_overview.md
- D-20260420-001 | トップカレンダーの `◯日前` は、root 画面の既存 indicator flow に混ぜず、日付セル anchor 直下の overlay として絶対配置する。`1日前増減` と `1日 / 7日前増減` の縦積みを壊さないことを優先する | status: applied | spec_link: docs/spec_001_analyze_expansion.md
- D-20260420-002 | 月次実績画面の custom booking curve は LT 基準を正とし、宿泊日基準の派生ではなく、予約日基準 chart を土台にした派生表示として設計する | status: applied | spec_link: docs/spec_000_overview.md
- D-20260420-003 | 月次実績画面の custom booking curve は別 userscript へ分離せず、既存 userscript のまま進める。既存 top / analyze の完成機能を巻き込まないよう、route 単位の起動境界、monthly-progress 専用 storage namespace、描画責務の局所化を前提に実装する | status: applied | spec_link: docs/spec_000_overview.md
- D-20260420-004 | 月次実績画面は UI 実装より先に、同一 userscript 内の独立 slice として土台を切る。`/monthly-progress` route では既存 top / analyze の observer / sync を停止し、monthly-progress 側は kill switch と namespaced storage adapter を先に持つ | status: applied | spec_link: docs/spec_000_overview.md
- D-20260420-005 | 月次実績画面の `/api/v1/booking_curve/monthly` は、取り逃がし防止のため batch-date 単位の write-only snapshot を IndexedDB へ保存し始める。初期 slice では read path を切り替えず、現行表示は API 正本のまま維持する | status: applied | spec_link: docs/spec_000_overview.md
- D-20260420-006 | 月次実績画面の `予約日 -> LT` 変換は、`booking_curve/monthly` snapshot を month-end anchor の LT 系列へ落とす純粋関数として扱う。現年系列は未観測 bucket と ACT を month-end 到達前に打ち切り、比較系列は前年または前々年を別 line として重ねられるようにする | status: applied | spec_link: docs/spec_000_overview.md
- D-20260420-007 | 月次実績画面の LT 横軸メモリは、日別 booking curve と同じバケット定義を使う。monthly の `予約日 -> LT` preview も同じ bucket end-date 集約で系列化し、x 軸の粒度だけ別物にしない | status: applied | spec_link: docs/spec_000_overview.md
- D-20260420-008 | 月次実績画面の最初の UI は、予約日基準 `販売客室数` chart 直下の独立 block とする。既存 Recharts chart は置き換えず、左に `販売客室数`、右に `販売単価` を置く 2 カラム chart section を差し込み、対象月から未来 3 か月を同時表示しつつ `前年 / 前々年` compare と hover tooltip を同 section 内で持つ | status: applied | spec_link: docs/spec_000_overview.md
